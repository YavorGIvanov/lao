[package]
name = "lao-capture"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
lao-capture-api = { path = "../../api/capture", version = "0.1.0" }

[lints]
workspace = true

[package.metadata.lao]
kind = "svc"
owner = "A09"
api = "lao-capture-api"
state = "capture"
isolate = "worker"
status = "stub"
