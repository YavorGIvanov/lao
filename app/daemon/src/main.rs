fn main() {
    let _ = (
        lao_gate::status(),
        lao_codex::status(),
        lao_claude::status(),
        lao_route::status(),
        lao_model::status(),
        lao_run::status(),
    );
}
